#include <iostream>
using namespace std;

int main()
{

    int sz, val, a;
    cin >> sz >> val >> a;
    int i = 0;

    // array ka size declare karke hi input lena padega nahi toh one by one input lo
    // int arr[sz];
    for (i = 0; i < sz; i++)
    {
        // arr[i] = val;
    }
    for (i = 0; i < sz; i++)
    {
        // cout << arr[i];
    }

    return 0;
}